<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="40dp"
    android:paddingRight="40dp"
    tools:context=".MainMenuActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_gravity="center"
        android:src="@drawable/logo"
        />

    <Button
        android:id="@+id/bCompareProfiles"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginTop="50dp"
        android:text="Compara profilele informatice"
        app:layout_constraintTop_toBottomOf="@+id/logo" />
    <Button
        android:id="@+id/bJobs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:text="Posibile slujbe in domeniul informsticii"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/bCompareProfiles"/>

    <Button
        android:id="@+id/bQuiz"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:text="Scurt Quiz cunostinte de baza"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/bJobs"/>

    <Button
        android:id="@+id/bUniversities"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:text="Lista Unversitati/Facultati in jurul Pitestiului"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/bQuiz"/>

</androidx.constraintlayout.widget.ConstraintLayout>